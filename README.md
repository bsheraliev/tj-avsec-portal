# AvSec Portal

Статическая сборка портала (данные — только в шифрованном виде, вход по коду доступа). Исходники и данные — в приватном репозитории `bsheraliev/avsec-portal`; сюда копирует `tools/deploy.mjs`.
